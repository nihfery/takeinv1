[www]

; The official image enables a request access log in docker.conf whose default
; %r field can contain signed query tokens. Nginx already emits the canonical
; queryless HTTP access record, so suppress the duplicate FPM request log while
; retaining the image's stderr error log and captured worker output.
access.log = /dev/null
